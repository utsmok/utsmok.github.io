<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Link to Bootstrap CDN -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Link to custom CSS file -->
    <link rel="stylesheet" href="style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tabulator/5.0.5/css/tabulator.min.css" rel="stylesheet">
    <!-- Link to Lightbox CDN -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.3/js/lightbox.min.js"></script>
    <title>{{Title}}</title> <!-- Use the name field as the title -->
</head>
<body>
    <div class="container">
        <!-- Navbar with back button and title -->
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#" onclick="window.history.back()">Back</a> <!-- Close the window when clicked -->
            <span class="navbar-text ml-auto">{{Title}}</span> <!-- Use the name field as the navbar text -->
        </nav>
        <!-- Div for the image and details -->
        <div class="row mt-4">
            <!-- Div for the image -->
            <div class="col-md-6">
                <!-- Use the image field as the src and alt attributes -->
                <img src="{{image}}" alt="{{Title}}" class="img-fluid lightbox-image" data-lightbox="detail" data-title="{{Title}}">
            </div>
            <!-- Div for the details -->
            <div class="col-md-6">
                <!-- Use a list group to display the details -->
                <ul class="list-group list-group-flush">
                    <!-- Use the category field as the list item text -->
                    <li class="list-group-item">Category: {{Category}}</li>
                    <!-- Use the price field as the list item text -->
                    <li class="list-group-item">Name: {{Title}}</li>
                    <!-- Use the rating field as the list item text -->
                    <li class="list-group-item">Description: {{Description}}</li>
                    <!-- Use the description field as the list item text -->
                    <li class="list-group-item">URL: {{Adress}}</li>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>